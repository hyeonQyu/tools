# CLAUDE.md

Claude Code entrypoint for this repository.

## Document Map

- Shared implementation/workflow rules (all agents): `AGENTS.md`
- Architecture/domain reference (all agents): `docs/ARCHITECTURE.md`

## Commands

```bash
yarn dev          # Start dev server
yarn build        # Type-check + Vite build
yarn type         # Type-check only (tsc --noEmit)
yarn lint         # ESLint (zero-warnings policy)
yarn lint:fix     # ESLint with auto-fix
```

No test runner is configured.
